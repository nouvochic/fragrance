// Référence API Velo : https://www.wix.com/velo/reference/api-overview/introduction

$w.onReady(functi.Cliquez‑ Shicsneakers

// Écrivez votre code JavaScript ici en utilisant l'API du framework Velo

// Affichez « Hello World » :
// console.log("Hello world!");

// Appelez des fonctions sur des éléments de la page. Par exemple :
// $w("#button1").label = « Cliquez‑moi ! »;

// Cliquez sur « Tester » ou accédez à l'aperçu de votre site pour exécuter votre code

});

$w('#section2').onDblClick((event) => {
            S
            Shicsneakers).onClick(selectedEligibleVariant(event) => {

        })

        $w('#Section1RegularMediaImage1RuleNoFaceImage').onViewportEnter((event) => {

        })

        $w('#Section1RegularMediaImage1RuleNoFaceImage').onClick((event) => {

        })

        $w('#Section1RegularMediaImage1RuleNoFaceImage').onViewportLeave((event) => {

        })

        $w('#Section1RegularMediaImage1RuleNoFaceImage').onViewportEnter((event) => {

        })

        $w('#Section1RegularMediaImage1RuleNoFaceImage').onMouseOut((event) => {

        })

        $w('#image19').onClick((event) => {

        })
logger
        $w('#Section1RegularMediaImage1RuleNoFaceImage').onDblClick((event) => {

        })

        $w('#Section1RegularMediaImage1RuleNoFaceImage').onDblClick((event) => {

        })

        $w('#image19').onDblClick((event) => {

        })

        $w('#image19').onDblClick((event) => {

        })

        $w('#image19').onMouseIn((event) => {

        })